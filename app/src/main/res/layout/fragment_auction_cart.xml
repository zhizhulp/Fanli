<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    tools:context="com.ascba.rebate.fragments.auction.AuctionCartFragment">
    <com.ascba.rebate.view.MoneyBar
        app:textTitle="购物车"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
    <android.support.design.widget.TabLayout
        android:background="@color/white"
        app:tabIndicatorColor="@color/shop_red_text_color"
        app:tabMode="fixed"
        app:tabSelectedTextColor="@color/shop_red_text_color"
        app:tabTextColor="@color/main_text_normal"
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="50dp"/>
    <android.support.v4.view.ViewPager
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
</LinearLayout>
